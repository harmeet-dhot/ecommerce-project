import { ShoppingFadValidators } from './shopping-fad-validators';

describe('ShoppingFadValidators', () => {
  it('should create an instance', () => {
    expect(new ShoppingFadValidators()).toBeTruthy();
  });
});
